# ---------- Core numerical & plotting ----------
numpy>=1.26,<2.0          
scipy>=1.10
pandas>=1.5
matplotlib>=3.7

# ---------- Image I/O & processing ----------
tifffile>=2023.7
scikit-image>=0.21

# ---------- Machine learning / statistics ----------
scikit-learn>=1.3

# ---------- Widgets & notebook utils ----------
ipympl>=0.9
ipywidgets>=8.0
tqdm>=4.66
IPython>=8.10

# ---------- Parallelisation & extras ----------
joblib>=1.3
pooch>=1.8                 # for robust cached downloads

# ---------- DL stack -----------
tensorflow==2.17.*         # CPU build; stable with tf.keras API
keras<3.9.*                
h5py>=3.8,<4               # safe with TF 2.15+ checkpoints
segmentation-models==1.0.1 # uses tf.keras (set SM_FRAMEWORK=tf.keras)             

# ---------- OpenCV (no GUI deps; good for notebooks/CI) ----------
opencv-python-headless>=4.8

# ---------- Optional but useful ----------
# jupyterlab>=4.0
# notebook>=7.0


